import 'package:flutter/material.dart';

class AppColors {
  // Ana renkler — koyu lacivert + altın
  static const primary = Color(0xFF1B2A4A);
  static const primaryLight = Color(0xFF2E4080);
  static const primaryDark = Color(0xFF0D1B2A);

  // Vurgu rengi — altın
  static const accent = Color(0xFFC9A84C);
  static const accentLight = Color(0xFFE8C96A);

  // Arka plan ve yüzey
  static const background = Color(0xFFF6F4F1);
  static const surface = Color(0xFFFFFFFF);
  static const cardBg = Color(0xFFFFFFFF);

  // Yazı renkleri
  static const textPrimary = Color(0xFF1A1A2E);
  static const textSecondary = Color(0xFF6B7280);
  static const textLight = Color(0xFF9CA3AF);

  // Durum renkleri
  static const success = Color(0xFF059669);
  static const warning = Color(0xFFD97706);
  static const error = Color(0xFFDC2626);

  // Dosya durum renkleri
  static const statusAktif = Color(0xFF059669);
  static const statusBeklemede = Color(0xFFD97706);
  static const statusKapali = Color(0xFF6B7280);

  // Gradient — splash ve header için
  static const gradientStart = Color(0xFF1B2A4A);
  static const gradientEnd = Color(0xFF2E4080);
}
